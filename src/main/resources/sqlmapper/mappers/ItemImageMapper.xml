<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cucumber.market.api.mapper.item.ItemImageMapper">

    <insert id="insertImage" useGeneratedKeys="true" keyProperty="imageId">
        insert into item_image (original_name, key_name, item_id, idx)
        values (#{originalName}, #{keyName}, #{itemId}, #{index})
    </insert>

    <select id="selectImageKeyNames" resultType="java.lang.String">
        select key_name
        from item_image
        where item_id = #{itemId}
        order by idx
    </select>

    <select id="selectRepImageKeyName" resultType="java.lang.String">
        select key_name
        from item_image
        where item_id = #{itemId}
        and idx = 0
    </select>

    <update id="updateImageIndex">
        update item_image
        set idx = #{index}
        where key_name = #{keyName}
    </update>

    <delete id="deleteImage">
        delete from item_image
        where key_name = #{keyName}
    </delete>

    <delete id="deleteImages">
        delete from item_image
        where item_id = #{itemId}
    </delete>

</mapper>

