<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cucumber.market.api.mapper.goods.GoodsMapper">

    <insert id="save" useGeneratedKeys="true" keyProperty="id">
        insert into goods (member_id, goods_name, goods_info, price, post_date)
        values (#{memberId}, #{request.goodsName}, #{request.goodsInfo} ,#{request.price}, #{request.postDate, jdbcType=TIMESTAMP})
    </insert>
</mapper>